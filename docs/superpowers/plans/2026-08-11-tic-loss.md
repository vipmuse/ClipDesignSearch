# TIC (텍스트 모달 내부 대조) 구현 Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 유사도가 높은 **서로 다른** 제목끼리 텍스트 임베딩 공간에서 밀어내는 보조 손실을 추가해, "Container"와 "Beverage container"가 붙어 있어 T2I가 둘을 가르지 못하는 문제를 완화하는 `tic` 학습 방법을 만든다.

**Architecture:** 손실은 `src/train.py`의 기존 손실들(`masked_clip_loss`, `supcon_loss`) 옆에 순수 함수로 추가한다. 배치의 텍스트 임베딩만 보는 함수라 GPU·모델 없이 단위 테스트할 수 있다. 어떤 쌍을 밀어낼지 고르려면 배치 안에서 "제목이 같은지"를 알아야 하므로 `Collator`에 `text_label`을 되살리되, **원문 텍스트 기준으로 계산하고 오직 필터로만 쓴다.**

**Tech Stack:** Python 3, PyTorch(cu128), transformers/peft, PyYAML, pytest

**근거 문서:** `docs/superpowers/specs/2026-08-10-training-method-comparison-design.md` §3.2

## Global Constraints

- 플랫폼은 Windows. 파이썬은 반드시 `.venv/Scripts/python.exe`를 쓴다. 전역 파이썬의 torch는 CPU 빌드라 사용 금지.
- 주석·로그·커밋 메시지는 한국어. 기존 코드 스타일(간결한 한국어 주석, 왜를 설명)을 따른다.
- 새 의존성을 추가하지 않는다.
- 테스트는 GPU·모델 로딩 없이 도는 것만 작성한다. 모델이 필요한 검증은 스모크로 미룬다.
- 메서드 정의는 `configs/methods/<name>.yaml` 한 장 + `src/registry.py` 병합이 유일한 경로다.
- `tic`은 baseline 위에 손실 축 하나만 바꾼다. 그래야 ablation의 Δ가 이 손실의 기여로 읽힌다.
- `text_label`은 **절대 positive 판정에 쓰지 않는다** (§아래 배경 참조).

---

## 배경 — 왜 이 손실이 이 데이터에 맞는가

실측(2026-08):

```
472,615 레코드 / 고유 제목 28,859개
그중 27,412개(95%)가 다른 제목과 헤드 명사를 공유 → 레코드 457,100건 = 96.7%
예: Container | Beverage container | Food container | Storage container
    Shoe | Ballet shoe | Portion of a shoe | Blue sole and midsole assembly for single-sole shoe
```

"비슷하지만 다른" 제목이 예외가 아니라 표준이다. 이런 제목들이 텍스트 공간에서 붙어 있으면
텍스트로 검색할 때 구분이 되지 않는다. TIC은 이 쌍들을 밀어낸다.

**완전히 같은 제목 쌍은 반드시 제외한다.** 같은 문자열은 같은 토큰이고 같은 인코더를 지나므로
**임베딩이 정확히 같은 벡터**다. 떼어놓는 것이 수학적으로 불가능하고, 억지로 밀면 인코더만
망가진다. FG-CLIP 2의 TIC이 말하는 "filtered hard negatives among high-similarity text pairs"의
필터가 바로 이것이다.

**같은 `design_id` 쌍도 제외한다.** 같은 디자인의 다른 뷰는 텍스트 공간에서 붙어 있는 것이 맞다.

**스펙 §3.2와의 의도적 편차 — 필터 기준.** 스펙은 "유사도가 지나치게 높은(= 사실상 같은
물품명칭) 쌍은 네거티브에서 제외한다"고 적었다. 즉 **유사도 임계값** 기반 필터다. 본 계획은
**문자열 동일성** 기반으로 바꾼다.

이유: 유사도로 거르면 TIC이 노려야 할 바로 그 쌍이 함께 걸러진다. "Container"와
"Beverage container"는 유사도가 매우 높지만 **서로 다른 물품**이고, 이들을 갈라놓는 것이 이
방법의 목적이다. 유사도 임계값을 그 위로 잡으면 아무것도 안 걸리고, 아래로 잡으면 목표 쌍이
사라진다. 반면 분리가 **원리적으로 불가능한** 쌍은 문자열이 같은 쌍뿐이다(같은 입력 → 같은
출력). 따라서 정확한 필터는 문자열 동일성이고, 유사도는 필터가 아니라 `margin`으로 "이미
충분히 떨어진 쌍은 건드리지 않는다"에 쓴다.

> **한계 명시**: FG-CLIP 2 전문의 TIC 정의(정확한 목적함수·온도·필터 기준)는 확보하지 않았다.
> 본 구현은 **원리 차용**이며, 논문 입수 시 교체 지점은 `tic_loss` 본체 한 곳이다.
> `tic_margin` 기본값 0.9는 L2 정규화 임베딩의 코사인 범위를 근거로 정한 값이지 실측값이 아니다.

## `text_label` 재도입에 대하여

Phase 0에서 `text_label`을 **제거**했다. 당시 문제는 이것이 `_pos_mask`에서 **positive 근거**로
쓰여, 고유 제목이 141개뿐인 데이터에서 마스크가 거의 전부 True가 되고 학습 신호가 사라진
것이었다. 지금 되살리는 것은 정반대 용도다 — **밀어낼 쌍에서 빼기 위한 필터**다.

되살리면서 당시의 부수 결함도 함께 막는다: 옛 구현은 증강된 텍스트(30% 확률로 viewpoint가
붙은)로 라벨을 만들어 같은 제목이 배치마다 다른 라벨을 받았다. 이번에는 **원문 `r["text"]`로만**
계산한다. Task 2가 이 두 가지를 테스트로 고정한다.

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `src/train.py` (수정) | `tic_loss` 추가 — 기존 손실들과 같은 자리 |
| `src/dataset.py` (수정) | `Collator`가 원문 기준 `text_label` 재생성 |
| `configs/methods/tic.yaml` (신규) | baseline + `tic_weight` |
| `tests/test_tic.py` (신규) | 손실의 필터·단조성·안정성 |

`src/tic.py`를 따로 만들지 않는 이유: `tic_loss`는 배치 텐서만 받는 순수 함수라
`masked_clip_loss`·`supcon_loss`와 성격이 같다. HOBIT이 별도 모듈이 된 것은 임베딩 계산이라는
다른 성격의 결합 때문이었고, 여기에는 그런 결합이 없다.

---

### Task 1: `tic_loss` 구현

**Files:**
- Modify: `src/train.py` (`supcon_loss` 아래에 추가)
- Create: `tests/test_tic.py`

**Interfaces:**
- Consumes: 없음 (torch만)
- Produces: `tic_loss(text_embeds, design_label, text_label, margin=0.9) -> torch.Tensor` — 스칼라. 대상 쌍이 없으면 0을 반환한다.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_tic.py` 생성:

```python
"""TIC 손실: 무엇을 밀고 무엇을 빼는지 고정한다.

핵심은 필터다 — 같은 제목 쌍은 임베딩이 같은 벡터라 분리 불가능하고,
같은 design_id 쌍은 붙어 있는 것이 맞다. 둘 다 손실에서 빠져야 한다.
"""
import os
import sys

import torch

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))
from train import tic_loss  # noqa: E402


def _emb(vectors):
    """행 단위 L2 정규화된 [B, D] 텐서."""
    t = torch.tensor(vectors, dtype=torch.float32)
    return t / t.norm(dim=-1, keepdim=True)


def test_제목이_같으면_밀어내지_않는다():
    """같은 문자열 → 같은 임베딩. 분리가 불가능하므로 손실 0이어야 한다."""
    e = _emb([[1.0, 0.0], [1.0, 0.0]])              # 유사도 1.0
    design = torch.tensor([0, 1])                    # 서로 다른 디자인
    text = torch.tensor([0, 0])                      # 같은 제목
    assert tic_loss(e, design, text, margin=0.5).item() == 0.0


def test_같은_design_id면_밀어내지_않는다():
    e = _emb([[1.0, 0.0], [0.99, 0.14]])
    design = torch.tensor([3, 3])                    # 같은 디자인
    text = torch.tensor([0, 1])                      # 다른 제목
    assert tic_loss(e, design, text, margin=0.5).item() == 0.0


def test_다른_디자인_다른_제목이_가까우면_양수_손실():
    e = _emb([[1.0, 0.0], [1.0, 0.02]])              # 유사도 ≈ 1.0
    design = torch.tensor([0, 1])
    text = torch.tensor([0, 1])
    assert tic_loss(e, design, text, margin=0.5).item() > 0.0


def test_margin_이하로_떨어져_있으면_손실_0():
    e = _emb([[1.0, 0.0], [0.0, 1.0]])               # 유사도 0.0
    design = torch.tensor([0, 1])
    text = torch.tensor([0, 1])
    assert tic_loss(e, design, text, margin=0.5).item() == 0.0


def test_가까울수록_손실이_커진다():
    design = torch.tensor([0, 1])
    text = torch.tensor([0, 1])
    near = tic_loss(_emb([[1.0, 0.0], [1.0, 0.05]]), design, text, margin=0.5)
    far = tic_loss(_emb([[1.0, 0.0], [1.0, 0.60]]), design, text, margin=0.5)
    assert near.item() > far.item() > 0.0


def test_대상_쌍이_하나도_없으면_0을_반환하고_NaN이_아니다():
    e = _emb([[1.0, 0.0], [0.0, 1.0]])
    design = torch.tensor([5, 5])                    # 전부 같은 디자인 → 대상 없음
    text = torch.tensor([0, 0])
    out = tic_loss(e, design, text, margin=0.5)
    assert out.item() == 0.0 and torch.isfinite(out)


def test_그래디언트가_텍스트_임베딩으로_흐른다():
    e = _emb([[1.0, 0.0], [1.0, 0.05]]).requires_grad_(True)
    loss = tic_loss(e, torch.tensor([0, 1]), torch.tensor([0, 1]), margin=0.5)
    loss.backward()
    assert e.grad is not None and e.grad.abs().sum().item() > 0


def test_배치가_커도_대칭_쌍을_두_번_세지_않는다():
    """상삼각만 세는지 확인 — 같은 쌍을 두 번 세면 평균이 왜곡되지는 않지만
    쌍 수 기반 통계를 나중에 붙일 때 어긋난다."""
    e = _emb([[1.0, 0.0], [1.0, 0.05], [1.0, 0.10]])
    design = torch.tensor([0, 1, 2])
    text = torch.tensor([0, 1, 2])
    out = tic_loss(e, design, text, margin=0.5)
    assert torch.isfinite(out) and out.item() > 0
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_tic.py -v`
Expected: FAIL — `ImportError: cannot import name 'tic_loss' from 'train'`

- [ ] **Step 3: `tic_loss` 구현**

`src/train.py`의 `supcon_loss` 정의 바로 아래에 추가:

```python
def tic_loss(text_embeds, design_label, text_label, margin=0.9):
    """텍스트 모달 내부 대조(TIC). 가까이 붙은 '서로 다른' 제목 쌍만 밀어낸다.

    이 데이터의 핵심 난점은 제목이 거의 다 겹친다는 것이다 — 고유 제목 28,859개 중
    27,412개(레코드의 96.7%)가 다른 제목과 헤드 명사를 공유한다(2026-08 실측).
    'Container'와 'Beverage container'가 텍스트 공간에서 붙어 있으면 텍스트 검색이
    둘을 가르지 못한다.

    두 종류를 대상에서 뺀다:
    - 제목이 완전히 같은 쌍: 같은 문자열 → 같은 토큰 → 정확히 같은 벡터라 분리가
      불가능하다. 억지로 밀면 인코더만 망가진다.
    - 같은 design_id 쌍: 같은 디자인의 뷰는 붙어 있는 것이 맞다.

    margin 이하로 떨어진 쌍은 이미 충분히 구분되므로 손실에 기여하지 않는다.
    """
    t = F.normalize(text_embeds, dim=-1)
    sim = t @ t.t()
    B = t.size(0)
    upper = torch.triu(torch.ones(B, B, dtype=torch.bool, device=t.device), diagonal=1)
    eligible = upper & (design_label[:, None] != design_label[None, :]) \
                     & (text_label[:, None] != text_label[None, :])
    if not eligible.any():
        return text_embeds.new_tensor(0.0)
    return (sim[eligible] - margin).clamp(min=0).mean()
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_tic.py -v`
Expected: PASS (8 passed)

- [ ] **Step 5: 전체 스위트 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 101 + 8 = 109 passed

- [ ] **Step 6: 커밋**

```bash
git add src/train.py tests/test_tic.py
git commit -m "TIC 손실 추가 — 가까이 붙은 서로 다른 제목만 밀어낸다

고유 제목 27,412/28,859개가 다른 제목과 헤드 명사를 공유해(레코드 96.7%)
'Container'와 'Beverage container'가 텍스트 공간에서 구분되지 않는다.
제목이 완전히 같은 쌍은 임베딩이 같은 벡터라 분리 불가능하므로 대상에서 뺀다."
```

---

### Task 2: 원문 기준 `text_label` 재도입과 학습 루프 연결

**Files:**
- Modify: `src/dataset.py` (`Collator.__call__`)
- Modify: `src/train.py` (학습 루프)
- Create: `tests/test_text_label.py`

**Interfaces:**
- Consumes: `tic_loss` (Task 1)
- Produces: `Collator`가 배치에 `text_label` (`torch.long [B]`) 을 추가한다. 같은 **원문** 텍스트 → 같은 정수.

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_text_label.py` 생성. `Collator`는 HF processor를 요구하므로 가짜 processor를 쓴다:

```python
"""text_label 재도입: 원문 기준이며 positive 판정에는 쓰이지 않는다.

Phase 0에서 이 라벨을 제거한 이유는 _pos_mask가 이것을 positive 근거로 써서
마스크가 거의 전부 True가 됐기 때문이다. 지금 용도는 정반대 — TIC이 밀어낼
쌍에서 '제목이 같은 쌍'을 빼기 위한 필터다.
"""
import os
import sys

import torch
from PIL import Image

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))
from dataset import Collator  # noqa: E402


class _FakeProcessor:
    """텍스트 개수만 맞춰 최소 텐서를 돌려주는 가짜. 토크나이저를 로드하지 않는다."""

    def __call__(self, text=None, images=None, return_tensors=None, **kw):
        n = len(text)
        return {"input_ids": torch.zeros(n, 4, dtype=torch.long),
                "attention_mask": torch.ones(n, 4, dtype=torch.long),
                "pixel_values": torch.zeros(n, 3, 8, 8)}


def _records(texts, designs, viewpoints=None):
    return [{"image": f"{i}.png", "text": t, "design_id": d,
             "viewpoint": (viewpoints[i] if viewpoints else "")}
            for i, (t, d) in enumerate(zip(texts, designs))]


def _collate(records, image_root, augment=False):
    return Collator(_FakeProcessor(), 8, image_root, augment=augment)(records)


def test_같은_원문_제목은_같은_라벨(tmp_path):
    for i in range(3):
        Image.new("RGB", (8, 8), (255, 255, 255)).save(tmp_path / f"{i}.png")
    enc = _collate(_records(["Shoe", "Shoe", "Bottle"], ["A", "B", "C"]), str(tmp_path))
    tl = enc["text_label"].tolist()
    assert tl[0] == tl[1] and tl[0] != tl[2]


def test_증강이_켜져도_라벨은_원문_기준이다(tmp_path):
    """옛 구현은 viewpoint가 붙은 텍스트로 라벨을 만들어 같은 제목이 배치마다
    다른 라벨을 받았다. 증강 확률과 무관하게 라벨이 갈리지 않아야 한다."""
    for i in range(2):
        Image.new("RGB", (8, 8), (255, 255, 255)).save(tmp_path / f"{i}.png")
    recs = _records(["Shoe", "Shoe"], ["A", "B"], viewpoints=["front view", "side view"])
    labels = set()
    for _ in range(30):                       # 증강은 확률적 → 여러 번 돌려 확인
        enc = _collate(recs, str(tmp_path), augment=True)
        tl = enc["text_label"].tolist()
        assert tl[0] == tl[1], "증강 때문에 같은 제목이 다른 라벨을 받았다"
        labels.add(tuple(tl))
    assert labels == {(0, 0)}


def test_design_label도_함께_나온다(tmp_path):
    for i in range(2):
        Image.new("RGB", (8, 8), (255, 255, 255)).save(tmp_path / f"{i}.png")
    enc = _collate(_records(["Shoe", "Bottle"], ["A", "A"]), str(tmp_path))
    assert enc["design_label"].tolist() == [0, 0]


def test_pos_mask는_text_label을_받지_않는다():
    """Phase 0의 회귀 방지 — 제목 동일성이 positive 근거로 돌아오면 안 된다."""
    import inspect

    from train import _pos_mask
    assert list(inspect.signature(_pos_mask).parameters) == ["design_label"]
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_text_label.py -v`
Expected: FAIL — `KeyError: 'text_label'` (앞 세 테스트). 마지막 테스트는 통과한다.

- [ ] **Step 3: `Collator`가 원문 기준 `text_label`을 만들도록 수정**

`src/dataset.py`의 `Collator.__call__`에서, 원문 텍스트를 따로 모은다. `texts.append(text)` 하는 자리 근처에 원문 리스트를 함께 쌓는다 — 즉 증강 전 값을 보관한다:

```python
        images, texts, raw_texts, design_ids = [], [], [], []
```

루프 안에서 증강 **전에** 원문을 담는다:

```python
            text = r["text"]
            raw_texts.append(text)                    # 라벨은 원문 기준 — 증강 전 값
            if self.augment and r.get("viewpoint") and random.random() < 0.3:
                text = f"{text}, {r['viewpoint']}"     # 텍스트 다양화 (④)
            texts.append(text)
```

`design_label`을 만드는 블록 아래에 추가:

```python
        # 원문 제목이 같으면 같은 정수. TIC이 '밀어낼 수 없는 쌍'을 빼는 필터로만 쓴다.
        # positive 판정에는 절대 쓰지 않는다 — 고유 제목이 141개뿐이라 그렇게 쓰면
        # 서로 다른 디자인이 정답으로 묶여 학습 신호가 사라진다 (Phase 0에서 제거한 이유).
        uniq_t = {}
        enc["text_label"] = torch.tensor(
            [uniq_t.setdefault(t, len(uniq_t)) for t in raw_texts], dtype=torch.long)
```

`Collator` 독스트링에도 한 줄 더한다:

```python
    design_label: 같은 design_id를 positive로 묶는 멀티-positive loss용 정수 라벨.
    text_label: 같은 원문 제목 → 같은 정수. TIC 필터 전용 (positive 판정에 쓰지 않음).
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_text_label.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 학습 루프에 TIC 연결**

`src/train.py`의 학습 루프에서 `design_label`을 꺼내는 줄 다음에 `text_label`도 꺼낸다:

```python
            design_label = enc.pop("design_label").to(device)
            text_label = enc.pop("text_label").to(device)
```

`evaluate()`에서도 `text_label`을 꺼낸다. 이건 정확성 문제가 아니라 정리 차원이다 — 학습 루프와
`evaluate()` 모두 `model(input_ids=…, attention_mask=…, pixel_values=…)`로 **명시 인자만** 넘기므로
남아 있어도 모델에 전달되지 않는다. 다만 `enc = {k: v.to(device) …}`가 쓰지도 않을 텐서를 GPU로
옮기게 되므로 꺼내는 편이 낫다. `d = enc.pop("design_label").to(device)` 다음 줄에 추가:

```python
        enc.pop("text_label", None)                    # 평가에는 쓰지 않는다
```

손실 합성부에 TIC 항을 더한다 (`img2img_weight` 블록 바로 아래):

```python
                if t.get("tic_weight", 0.0) > 0:
                    loss = loss + t["tic_weight"] * tic_loss(
                        out.text_embeds, design_label, text_label,
                        margin=t.get("tic_margin", 0.9))
```

- [ ] **Step 6: 전체 스위트 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 109 + 4 = 113 passed

- [ ] **Step 7: 기존 방법이 깨지지 않았는지 스모크**

Run: `./.venv/Scripts/python.exe src/train.py --limit 300 --max-steps 3 --eval-batches 1`
Expected: 예외 없이 3스텝 진행. `KeyError`나 `TypeError`가 나면 `text_label`을 꺼내지 않은 경로가 남은 것이다. 이 실행은 `configs/lora_clip.yaml` 기본값이라 `tic_weight`가 없어 TIC 항은 꺼져 있다 — 즉 기존 방법들의 동작이 그대로인지 보는 것이 목적이다.

- [ ] **Step 8: 커밋**

```bash
git add src/dataset.py src/train.py tests/test_text_label.py
git commit -m "원문 기준 text_label 재도입 + TIC 항을 학습 루프에 연결

Phase 0에서 제거했던 라벨을 되살리되 용도가 정반대다 — positive 근거가 아니라
TIC이 '밀어낼 수 없는 쌍'(제목이 완전히 같은 쌍)을 빼기 위한 필터다.
옛 구현의 부수 결함도 막는다: 증강된 텍스트가 아니라 원문으로 계산해
같은 제목이 배치마다 다른 라벨을 받지 않는다."
```

---

### Task 3: `tic` 메서드 등록과 문서

**Files:**
- Create: `configs/methods/tic.yaml`
- Modify: `tests/test_method_configs.py`
- Modify: `ACCURACY.md`

**Interfaces:**
- Consumes: `registry.resolve` (기존), `tic_loss`·`text_label` (Task 1·2)
- Produces: 메서드 이름 `tic`. `run_ablation.py --arms baseline tic`으로 실행 가능.

- [ ] **Step 1: 메서드 YAML 작성**

`configs/methods/tic.yaml`:

```yaml
name: tic
description: "텍스트 모달 내부 대조 — 가까이 붙은 서로 다른 제목을 밀어낸다 (baseline + 손실 축만 변경)"
extends: configs/lora_clip.yaml
data: {builder: shared, pairs: data/pairs.jsonl}
train:
  tic_weight: 0.2
  tic_margin: 0.9
  pk_views: 1
  locarno_aware: false
  mask_false_negatives: false
  augment: false
  img2img_weight: 0.0
```

`pk_views` 이하 다섯 값은 `baseline.yaml`과 동일하다. `tic`이 바꾸는 것은 손실뿐이다.

- [ ] **Step 2: 단일축 검증 테스트 추가**

`tests/test_method_configs.py` 끝에 추가:

```python
def test_tic은_baseline과_손실_항만_다르다():
    """tic이 바꾸는 축은 손실 하나여야 Δ가 그 기여로 읽힌다."""
    c = registry.resolve("tic")["train"]
    b = registry.resolve("baseline")["train"]
    five = ("pk_views", "locarno_aware", "mask_false_negatives", "augment", "img2img_weight")
    assert [c[k] for k in five] == [b[k] for k in five]
    assert c["tic_weight"] > 0
    assert b.get("tic_weight", 0.0) == 0.0, "baseline에 TIC이 켜져 있으면 비교가 무의미"


def test_tic과_hobit은_서로_다른_축을_바꾼다():
    """두 방법이 같은 축을 건드리면 기여도가 섞인다."""
    tic = registry.resolve("tic")["train"]
    hobit = registry.resolve("hobit")["train"]
    assert tic.get("sampler", "pk") != "hobit", "tic이 배치 구성까지 바꾸고 있다"
    assert hobit.get("tic_weight", 0.0) == 0.0, "hobit이 손실까지 바꾸고 있다"
```

- [ ] **Step 3: 테스트 실행**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_method_configs.py -v`
Expected: PASS. 파라미터화 테스트가 메서드 12개를 덮는다.

- [ ] **Step 4: 스모크 — 실제로 학습이 도는지**

Run:

```bash
PYTHONUNBUFFERED=1 ./.venv/Scripts/python.exe scripts/run_ablation.py --arms tic --limit 400 --epochs 1 --max-steps 5 --eval-batches 1 --no-index --force
```

**반드시 Bash 툴의 `run_in_background: true`로 실행한다.** 포그라운드 툴 콜 안에서 띄우면 턴이 끝날 때 프로세스 트리가 정리되어 `train.log`가 0바이트로 남는다 (HOBIT 작업에서 두 번 겪은 실패다).

Expected: 예외 없이 5스텝 진행 후 평가·어댑터 저장. `outputs/methods/tic/final/`이 생긴다.
`out.text_embeds`가 없다는 `AttributeError`가 나면 모델 출력 필드명을 확인한다 — 확인된 바로는 `MetaClip2Output.text_embeds`가 존재한다.

- [ ] **Step 5: `ACCURACY.md` arm 표에 tic 추가**

기본 arm 표 끝, "선택 arm" 표 앞에 행을 추가한다:

```
| `tic` | baseline + 텍스트 모달 내부 대조 | ③ 제목 중복(27,412/28,859)이 이 데이터의 핵심 난점 |
```

실행 예시도 한 줄 덧붙인다:

```
python scripts\run_ablation.py --arms baseline tic --epochs 3   # 손실 축 단독 기여
```

- [ ] **Step 6: 전체 스위트 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 약 117 passed (파라미터화 테스트가 메서드 12개를 덮으며 tic이 2개, 신규 단언이 2개 더한다)

- [ ] **Step 7: 커밋**

```bash
git add configs/methods/tic.yaml tests/test_method_configs.py ACCURACY.md
git commit -m "tic 메서드 등록 — baseline 대비 손실 축만 변경

tic_weight 외 다섯 토글을 baseline과 동일하게 두어 비교표의 Δ가 곧 이 손실의
기여가 되도록 한다. hobit과 다른 축을 바꾸는지도 테스트로 고정한다."
```

---

## 완료 기준

- `./.venv/Scripts/python.exe -m pytest tests/ -v` 전부 통과 (약 117개)
- `run_ablation.py --arms baseline tic`이 두 arm을 같은 홀드아웃에서 학습·평가
- `tic`이 baseline과 손실 항 하나만 다르고, `hobit`과 다른 축을 바꾼다는 것이 테스트로 고정됨

## 알려진 한계

- **원리 차용이다.** FG-CLIP 2의 TIC 원 정의(목적함수·온도·필터 기준)는 확보하지 않았다.
  교체 지점은 `tic_loss` 본체 한 곳이다.
- **`tic_margin` 0.9와 `tic_weight` 0.2는 실측값이 아니다.** 정규화 임베딩의 코사인 범위를
  근거로 정한 초기값이며, 첫 ablation 결과를 보고 조정해야 한다. margin이 너무 낮으면 정상적으로
  구분된 쌍까지 밀어내 정렬을 해치고, 너무 높으면 손실이 대부분 0이 되어 아무 일도 하지 않는다.
- **밀어내기 전용 항이다.** 텍스트 쪽에 자연스러운 positive 쌍이 없어(제목은 짧고 패러프레이즈가
  없다) 대조 학습의 양방향 형태를 쓰지 못한다. 정렬은 주 CLIP 손실이 잡아주는 것에 의존한다.
  `tic_weight`가 과도하면 텍스트 공간이 흩어지며 T2I가 오히려 나빠질 수 있다.

## 다음 단계

남은 세 방법은 각각 별도 계획으로 다룬다. `loracap`은 `lora` 블록 오버라이드뿐이라 가장 짧고
(기존 `all-mlp`/`all-r32` arm이 이미 그 경로를 입증했다), `hires384`는
`facebook/metaclip-2-worldwide-huge-378` 다운로드와 `image_size: 378` 변경, `bigbatch`는
GradCache 구현이 필요하다.
