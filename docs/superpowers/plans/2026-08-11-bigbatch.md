# `bigbatch` (GradCache) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** GradCache로 대조학습의 **유효 네거티브 수**를 물리 배치와 분리해, VRAM을 늘리지 않고 배치 32 → 256으로 키운 `bigbatch` 방법을 만든다.

**Architecture:** 두 번 순전파한다. 1차는 `no_grad`로 청크별 임베딩만 모으고, 그 임베딩 위에서 손실을 계산해 임베딩에 대한 그래디언트를 얻는다. 2차는 청크별로 다시 순전파하며 1차에서 얻은 그래디언트를 주입해 역전파한다. 활성값을 청크 하나 분량만 들고 있으면 되므로 메모리는 물리 배치에 비례하고, 손실은 전체 유효 배치를 본다.

**Tech Stack:** Python 3, PyTorch(cu128), transformers/peft, PyYAML, pytest

**근거 문서:** `docs/superpowers/specs/2026-08-10-training-method-comparison-design.md` §3.3

## Global Constraints

- 플랫폼은 Windows. 파이썬은 반드시 `.venv/Scripts/python.exe`를 쓴다. 전역 파이썬의 torch는 CPU 빌드라 사용 금지.
- 주석·로그·커밋 메시지는 한국어. 기존 코드 스타일(간결한 한국어 주석, 왜를 설명)을 따른다.
- 새 의존성을 추가하지 않는다. GradCache 라이브러리를 쓰지 않고 직접 구현한다(수십 줄이고, 이 저장소의 손실 구성과 맞물려야 한다).
- 테스트는 GPU·모델 로딩 없이 도는 것만 작성한다. 모델이 필요한 검증은 스모크로 미룬다.
- **em 대시(U+2014)를 출력되거나 config `description`에 저장되는 곳에 쓰지 않는다.** 이 콘솔은 cp949라 인코딩할 수 없고, 이 저장소에서 학습을 두 번 죽였다. 아스키 하이픈을 쓴다.
- `bigbatch`는 baseline 위에 **유효 네거티브 수 축 하나만** 바꾼다. `batch_size`는 32 그대로 두고 청크 수로 유효 배치를 만든다.
- `bigbatch`는 `DEFAULT_ARMS`에 넣지 않는다 - `--arms`로만 선택하는 opt-in이다.

---

## 왜 이 방법이 필요한가

대조학습의 네거티브는 **물리 배치 안**에서 나온다. `grad_accum`을 올려도 네거티브는 늘지 않는다 -
손실이 배치마다 따로 계산되기 때문이다. 그래서 유효 배치를 키우려면 물리 배치를 키워야 하는데,
`configs/lora_clip.yaml`의 실측 주석대로 batch 64는 peak 37.5GB로 32GB를 넘겨 스필한다.

GradCache는 이 결합을 끊는다. 활성값을 청크 하나 분량만 유지하면서 손실은 전체를 본다.

이 방법이 먼저 들어와야 `hires378`이 성립한다는 점도 중요하다. 378 해상도는 토큰이 2.85배라
물리 배치 8이 한계인데, GradCache가 있으면 물리 8에서도 유효 네거티브 32를 유지할 수 있어
해상도 축만 바뀌는 비교가 가능해진다.

**모델의 logits 계산식** (확인 완료, `modeling_metaclip_2.py`):

```python
image_embeds = pooler_output / norm      # L2 정규화
text_embeds  = pooler_output / norm
logits_per_text  = (text_embeds @ image_embeds.t()) * logit_scale.exp()
logits_per_image = logits_per_text.t()
```

`logit_bias`가 없으므로 캐시된 임베딩만으로 logits를 **정확히** 재구성할 수 있다. 이것이 이
구현이 근사가 아니라 등가인 근거다.

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `src/gradcache.py` (신규) | 캐시된 임베딩에서 손실·그래디언트를 만드는 순수 함수 |
| `src/train.py` (수정) | 청크 누적 + 2패스 루프 |
| `configs/lora_clip.yaml` (수정) | `grad_cache_chunks: 1` 기본값 |
| `configs/methods/bigbatch.yaml` (신규) | baseline + `grad_cache_chunks: 8` |
| `tests/test_gradcache.py` (신규) | 그래디언트 등가성 |

`src/gradcache.py`를 따로 두는 이유: 이 함수는 배치 텐서만 받는 순수 함수라 장난감 인코더로
등가성을 검증할 수 있다. `train.py`에 인라인으로 넣으면 그 검증이 불가능해진다.

---

### Task 1: `cached_grads` - 캐시된 임베딩에서 손실과 그래디언트

**Files:**
- Create: `src/gradcache.py`
- Create: `tests/test_gradcache.py`

**Interfaces:**
- Consumes: `train.compose_loss(out, pos, t, design_label, text_label, head_label) -> (loss, stats)`
- Produces: `cached_grads(image_embeds, text_embeds, pos, t, design_label, text_label, head_label, logit_scale, compose_fn) -> (loss_value, grad_img, grad_txt, stats)`

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_gradcache.py` 생성. 핵심은 **등가성**이다 - 2패스가 단일 역전파와 같은 그래디언트를 내는지를 장난감 인코더로 검증한다(실제 모델 불필요):

```python
"""GradCache: 2패스가 단일 역전파와 같은 그래디언트를 내는지 고정한다.

이 방법의 존재 이유가 '메모리만 줄이고 수학은 그대로'이므로, 등가성이 깨지면
비교표의 Δ는 네거티브 수의 효과가 아니라 구현 오차가 된다.
"""
import os
import sys

import torch
import torch.nn as nn

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))
from gradcache import cached_grads  # noqa: E402
from train import compose_loss  # noqa: E402

T = {"img2img_weight": 0.0, "tic_weight": 0.0}          # 순수 CLIP 손실만


class _ToyEncoder(nn.Module):
    """이미지·텍스트를 각각 선형 변환 후 L2 정규화. 모델 대신 쓰는 최소 스탠드인."""

    def __init__(self, d_in=6, d_out=4):
        super().__init__()
        self.img = nn.Linear(d_in, d_out, bias=False)
        self.txt = nn.Linear(d_in, d_out, bias=False)

    def forward(self, xi, xt):
        i = torch.nn.functional.normalize(self.img(xi), dim=-1)
        t = torch.nn.functional.normalize(self.txt(xt), dim=-1)
        return i, t


def _fixture(n=8, d_in=6, seed=0):
    g = torch.Generator().manual_seed(seed)
    xi = torch.randn(n, d_in, generator=g)
    xt = torch.randn(n, d_in, generator=g)
    design = torch.arange(n)                             # 전부 다른 디자인
    text = torch.arange(n)
    head = torch.zeros(n, dtype=torch.long)
    pos = torch.eye(n, dtype=torch.bool)
    return xi, xt, design, text, head, pos


def _single_pass_grads(enc, logit_scale, xi, xt, pos, design, text, head):
    """비교 기준: 전체를 한 번에 순전파하고 한 번 역전파."""
    enc.zero_grad(set_to_none=True)
    if logit_scale.grad is not None:
        logit_scale.grad = None
    i, t = enc(xi, xt)
    logits = (i @ t.t()) * logit_scale.exp()
    from types import SimpleNamespace
    out = SimpleNamespace(logits_per_image=logits, image_embeds=i, text_embeds=t)
    loss, _ = compose_loss(out, pos, T, design, text, head)
    loss.backward()
    return float(loss), enc.img.weight.grad.clone(), enc.txt.weight.grad.clone(), \
        logit_scale.grad.clone()


def _gradcache_grads(enc, logit_scale, xi, xt, pos, design, text, head, n_chunks):
    """GradCache 2패스: 청크로 나눠 임베딩만 모으고, 손실 그래디언트를 주입해 역전파."""
    enc.zero_grad(set_to_none=True)
    if logit_scale.grad is not None:
        logit_scale.grad = None
    chunks = list(zip(xi.chunk(n_chunks), xt.chunk(n_chunks)))

    with torch.no_grad():                                # 1패스: 임베딩만
        embeds = [enc(a, b) for a, b in chunks]
    img = torch.cat([e[0] for e in embeds])
    txt = torch.cat([e[1] for e in embeds])

    _, g_img, g_txt, _ = cached_grads(img, txt, pos, T, design, text, head,
                                      logit_scale, compose_loss)

    off = 0                                              # 2패스: 재순전파 + 주입
    for a, b in chunks:
        i, t = enc(a, b)
        k = i.size(0)
        torch.autograd.backward([i, t], [g_img[off:off + k], g_txt[off:off + k]])
        off += k
    return enc.img.weight.grad.clone(), enc.txt.weight.grad.clone(), logit_scale.grad.clone()


def test_2패스_그래디언트가_단일_역전파와_같다():
    """이 방법의 존재 근거. 어긋나면 Δ가 구현 오차를 재게 된다."""
    xi, xt, design, text, head, pos = _fixture()
    enc = _ToyEncoder()
    ls = torch.tensor(2.6592, requires_grad=True)        # ln(100/7.2) 근처, 실제 초기값대
    base = {k: v.clone() for k, v in enc.state_dict().items()}

    _, gi_ref, gt_ref, gls_ref = _single_pass_grads(enc, ls, xi, xt, pos, design, text, head)
    enc.load_state_dict(base)
    gi, gt, gls = _gradcache_grads(enc, ls, xi, xt, pos, design, text, head, n_chunks=4)

    assert torch.allclose(gi, gi_ref, atol=1e-6), f"이미지 인코더 그래디언트 불일치 {(gi-gi_ref).abs().max()}"
    assert torch.allclose(gt, gt_ref, atol=1e-6), f"텍스트 인코더 그래디언트 불일치 {(gt-gt_ref).abs().max()}"
    assert torch.allclose(gls, gls_ref, atol=1e-6), "logit_scale 그래디언트 불일치"


def test_청크_수를_바꿔도_그래디언트가_같다():
    """청크 수는 메모리 손잡이일 뿐 수학을 바꾸지 않는다."""
    xi, xt, design, text, head, pos = _fixture()
    enc = _ToyEncoder()
    ls = torch.tensor(2.6592, requires_grad=True)
    base = {k: v.clone() for k, v in enc.state_dict().items()}
    outs = []
    for k in (1, 2, 4, 8):
        enc.load_state_dict(base)
        outs.append(_gradcache_grads(enc, ls, xi, xt, pos, design, text, head, n_chunks=k))
    for gi, gt, gls in outs[1:]:
        assert torch.allclose(gi, outs[0][0], atol=1e-6)
        assert torch.allclose(gt, outs[0][1], atol=1e-6)
        assert torch.allclose(gls, outs[0][2], atol=1e-6)


def test_손실이_전체_유효_배치를_본다():
    """청크 안이 아니라 전체에서 네거티브가 나와야 이 방법이 의미가 있다.
    8개를 4청크로 쪼갠 손실은, 2개짜리 배치 4개를 따로 계산한 손실과 달라야 한다."""
    xi, xt, design, text, head, pos = _fixture()
    enc = _ToyEncoder()
    ls = torch.tensor(2.6592, requires_grad=True)
    with torch.no_grad():
        i, t = enc(xi, xt)
    full, _, _, _ = cached_grads(i, t, pos, T, design, text, head, ls, compose_loss)

    per_chunk = []
    for s in range(0, 8, 2):
        sl = slice(s, s + 2)
        with torch.no_grad():
            ic, tc = enc(xi[sl], xt[sl])
        v, _, _, _ = cached_grads(ic, tc, pos[sl, sl], T, design[sl], text[sl], head[sl],
                                  ls, compose_loss)
        per_chunk.append(v)
    assert abs(full - sum(per_chunk) / len(per_chunk)) > 1e-3, \
        "전체 손실이 청크별 평균과 같다 - 네거티브가 청크 안에 갇혀 있다"


def test_stats가_compose_loss의_것을_그대로_전달한다():
    xi, xt, design, text, head, pos = _fixture()
    enc = _ToyEncoder()
    ls = torch.tensor(2.6592, requires_grad=True)
    with torch.no_grad():
        i, t = enc(xi, xt)
    _, _, _, stats = cached_grads(i, t, pos, T, design, text, head, ls, compose_loss)
    assert set(stats) == {"tic", "n_eligible", "n_violating"}
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_gradcache.py -v`
Expected: FAIL - `ModuleNotFoundError: No module named 'gradcache'`

- [ ] **Step 3: `src/gradcache.py` 구현**

```python
"""GradCache: 물리 배치와 유효 네거티브 수를 분리한다.

대조학습의 네거티브는 물리 배치 안에서 나오므로 grad_accum으로는 늘릴 수 없다.
활성값을 청크 하나 분량만 유지한 채 손실은 전체를 보게 하려면 두 번 순전파해야 한다:
1패스는 no_grad로 임베딩만 모으고, 그 위에서 손실을 계산해 임베딩에 대한 그래디언트를
얻는다. 2패스는 청크별로 다시 순전파하며 그 그래디언트를 주입해 역전파한다.

모델이 logits를 (image_embeds @ text_embeds.T) * logit_scale.exp()로 계산하고
logit_bias가 없으므로, 캐시된 임베딩만으로 logits를 정확히 재구성할 수 있다.
이 구현이 근사가 아니라 등가인 근거다 (tests/test_gradcache.py가 고정한다).
"""
from types import SimpleNamespace

import torch


def cached_grads(image_embeds, text_embeds, pos, t, design_label, text_label, head_label,
                 logit_scale, compose_fn):
    """캐시된 임베딩으로 손실을 계산하고 임베딩에 대한 그래디언트를 돌려준다.

    반환: (손실값 float, grad_img, grad_txt, stats)

    logit_scale은 여기서 그래디언트를 받는다 - 2패스에서 다시 계산하지 않으므로
    이 backward가 logit_scale의 유일한 갱신 경로다.
    """
    img = image_embeds.detach().requires_grad_(True)
    txt = text_embeds.detach().requires_grad_(True)
    logits_per_image = (img @ txt.t()) * logit_scale.exp()
    out = SimpleNamespace(logits_per_image=logits_per_image, image_embeds=img, text_embeds=txt)
    loss, stats = compose_fn(out, pos, t, design_label, text_label, head_label)
    loss.backward()
    return float(loss.detach()), img.grad, txt.grad, stats
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/test_gradcache.py -v`
Expected: PASS (4 passed)

등가성 테스트가 실패하면 구현이 아니라 테스트 하네스를 먼저 의심한다 - `logit_scale.grad`를 두 경로 사이에 초기화했는지, `load_state_dict`로 가중치를 되돌렸는지 확인한다.

- [ ] **Step 5: 등가성 테스트가 실제로 변별하는지 뮤테이션으로 확인**

`cached_grads`에서 `logits_per_image = (img @ txt.t()) * logit_scale.exp()`의 `* logit_scale.exp()`를 일시적으로 지우고 `test_2패스_그래디언트가_단일_역전파와_같다`가 **실패**하는지 확인한 뒤 원복한다. 두 결과와 `git diff src/gradcache.py`가 비어 있음을 리포트에 붙인다.

- [ ] **Step 6: 커밋**

```bash
git add src/gradcache.py tests/test_gradcache.py
git commit -m "GradCache 핵심: 캐시된 임베딩에서 손실과 그래디언트 계산

대조학습의 네거티브는 물리 배치 안에서 나와 grad_accum으로 늘릴 수 없다.
2패스로 활성값은 청크 분량만 유지하면서 손실은 전체 유효 배치를 보게 한다.
장난감 인코더로 단일 역전파와의 그래디언트 등가성을 고정했다."
```

---

### Task 2: 학습 루프에 2패스 연결

**Files:**
- Modify: `src/train.py`
- Modify: `configs/lora_clip.yaml`

**Interfaces:**
- Consumes: `cached_grads(...)` (Task 1)
- Produces: `train.grad_cache_chunks` 설정값. 1이면 기존 경로 그대로, >1이면 2패스.

- [ ] **Step 1: 설정 기본값 추가**

`configs/lora_clip.yaml`의 `train` 블록에 추가:

```yaml
  # GradCache: 청크 N개를 모아 유효 배치 batch_size*N으로 손실을 계산한다.
  # 1이면 비활성(기존 경로). 대조학습의 네거티브는 물리 배치 안에서 나오므로
  # grad_accum과 달리 이것만이 네거티브 수를 실제로 늘린다.
  grad_cache_chunks: 1
```

- [ ] **Step 2: 학습 루프에 2패스 분기 추가**

`src/train.py` import에 추가:

```python
from gradcache import cached_grads
```

`main()`에서 배치 인코딩을 한 곳으로 모으는 헬퍼를 손실 합성부 근처에 추가한다. 기존 루프가 `for i, enc in enumerate(train_loader):`로 배치를 하나씩 처리하므로, 청크 누적은 그 바깥에서 한다:

```python
def _encode_batch(model, enc, device, dtype):
    """배치 하나를 순전파해 (out, 라벨들)을 돌려준다. 2패스 양쪽에서 쓴다."""
    design_label = enc.pop("design_label").to(device)
    text_label = enc.pop("text_label").to(device)
    head_label = enc.pop("head_label").to(device)
    enc = {k: v.to(device) for k, v in enc.items()}
    with torch.autocast(device_type=device, dtype=dtype, enabled=(dtype != torch.float32)):
        out = model(input_ids=enc["input_ids"], attention_mask=enc["attention_mask"],
                    pixel_values=enc["pixel_values"])
    return out, design_label, text_label, head_label, enc
```

기존 루프 본문을 건드리지 않고, `grad_cache_chunks > 1`일 때만 도는 별도 경로를 만든다. 골격은 아래와 같다 - 기존 루프의 `max_steps`·`save_every`·스케줄러 호출 위치를 읽고 그에 맞춰 배치한다:

```python
        n_chunks = t.get("grad_cache_chunks", 1)
        if n_chunks > 1:
            group = []
            for enc in train_loader:
                group.append(enc)
                if len(group) < n_chunks:
                    continue                       # 자투리 그룹은 에폭 끝에서 버린다

                # 1패스: 임베딩만 모은다 (활성값은 청크 하나 분량만 유지).
                # 청크마다 RNG 상태를 저장한다 - lora.dropout이 0.05라 2패스가 다른
                # 드롭아웃 마스크를 보면 캐시한 그래디언트가 재계산된 순전파와 어긋난다.
                embeds, labels, rng_states = [], [], []
                with torch.no_grad():
                    for e in group:
                        rng_states.append((torch.get_rng_state(),
                                           torch.cuda.get_rng_state(device)
                                           if device == "cuda" else None))
                        out, d, tl, hl, _ = _encode_batch(model, dict(e), device, dtype)
                        embeds.append((out.image_embeds.detach(), out.text_embeds.detach()))
                        labels.append((d, tl, hl))
                img = torch.cat([a for a, _ in embeds])
                txt = torch.cat([b for _, b in embeds])
                design = torch.cat([d for d, _, _ in labels])
                text_l = torch.cat([x for _, x, _ in labels])
                head_l = torch.cat([x for _, _, x in labels])

                # pos는 반드시 연결된 전체 라벨로 - 청크별로 만들면 네거티브가 갇힌다
                pos = _pos_mask(design) if mask_fn \
                    else torch.eye(design.size(0), dtype=torch.bool, device=device)
                loss_val, g_img, g_txt, lstats = cached_grads(
                    img, txt, pos, t, design, text_l, head_l, logit_scale, compose_loss)

                # 2패스: 청크별 재순전파 + 그래디언트 주입.
                # 1패스에서 저장한 RNG 상태를 되돌려 같은 드롭아웃 마스크를 재현한다.
                off = 0
                for e, (cpu_state, cuda_state) in zip(group, rng_states):
                    torch.set_rng_state(cpu_state)
                    if cuda_state is not None:
                        torch.cuda.set_rng_state(cuda_state, device)
                    out, _, _, _, _ = _encode_batch(model, dict(e), device, dtype)
                    k = out.image_embeds.size(0)
                    torch.autograd.backward([out.image_embeds, out.text_embeds],
                                            [g_img[off:off + k], g_txt[off:off + k]])
                    off += k

                torch.nn.utils.clip_grad_norm_(lora_params, 1.0)
                optim.step(); sched.step(); optim.zero_grad()
                with torch.no_grad():
                    logit_scale.clamp_(max=math.log(100))
                step += 1
                group = []
                # 여기서 기존 루프와 같은 log_every / max_steps / save_every 처리를 한다
```

`dict(e)`로 감싸는 이유: `_encode_batch`가 `pop`으로 라벨을 꺼내므로 같은 배치를 2패스에서 다시 쓰려면 원본을 보존해야 한다.

**RNG 상태 보존은 선택이 아니다.** `configs/lora_clip.yaml`의 `lora.dropout`이 0.05라, 1패스와 2패스가 다른 드롭아웃 마스크를 보면 2패스가 재계산한 임베딩이 1패스가 캐시한 것과 달라진다. 그러면 주입하는 그래디언트가 엉뚱한 지점의 것이 되어 **등가성이 깨진다** - 소음이 아니라 오류다. `torch.utils.checkpoint`가 `preserve_rng_state=True`로 하는 일과 같다. Task 1의 등가성 테스트는 드롭아웃이 없는 장난감 인코더를 쓰므로 이 문제를 잡지 못한다 - 아래 Step 3-1이 그 구멍을 메운다.

지켜야 할 계약은 다음 네 가지다.

1. 청크 `N`개를 모아 **옵티마이저 스텝 1회**를 만든다 (`step` 증가도 1회).
2. 1패스는 `torch.no_grad()`로 임베딩만 모은다. `pos` 마스크는 **연결된 전체 라벨**로 만든다 (`_pos_mask(torch.cat(design_labels))`) - 청크별로 만들면 네거티브가 청크 안에 갇힌다.
3. 2패스는 청크별로 다시 순전파하고 `torch.autograd.backward([out.image_embeds, out.text_embeds], [g_img[sl], g_txt[sl]])`로 주입한다.
4. 마지막 자투리 그룹(청크가 `N`개 미만)은 버린다. 유효 배치 크기가 달라지면 그 스텝만 다른 조건이 된다.

`log_every` 로그에는 유효 배치 크기를 함께 찍는다:

```python
                    print(f"e{epoch} s{step}/{total_steps} loss={loss_val:.4f} "
                          f"eff_batch={t['batch_size'] * t['grad_cache_chunks']} ...")
```

- [ ] **Step 3-1: 드롭아웃이 있어도 등가성이 유지되는지 테스트**

Task 1의 등가성 테스트는 드롭아웃 없는 장난감 인코더를 써서 RNG 문제를 잡지 못한다. `tests/test_gradcache.py`에 드롭아웃이 있는 인코더로 같은 등가성을 확인하는 테스트를 추가한다:

```python
class _ToyEncoderDropout(nn.Module):
    """드롭아웃이 있는 스탠드인. 1패스와 2패스가 같은 마스크를 봐야 등가성이 성립한다."""

    def __init__(self, d_in=6, d_out=4, p=0.05):
        super().__init__()
        self.img = nn.Linear(d_in, d_out, bias=False)
        self.txt = nn.Linear(d_in, d_out, bias=False)
        self.drop = nn.Dropout(p)

    def forward(self, xi, xt):
        i = torch.nn.functional.normalize(self.drop(self.img(xi)), dim=-1)
        t = torch.nn.functional.normalize(self.drop(self.txt(xt)), dim=-1)
        return i, t


def test_드롭아웃이_있어도_RNG를_되돌리면_등가성이_유지된다():
    """RNG 상태를 보존하지 않으면 2패스가 다른 마스크를 봐 그래디언트가 어긋난다."""
    xi, xt, design, text, head, pos = _fixture()
    enc = _ToyEncoderDropout()
    enc.train()
    ls = torch.tensor(2.6592, requires_grad=True)
    base = {k: v.clone() for k, v in enc.state_dict().items()}

    torch.manual_seed(1234)
    _, gi_ref, gt_ref, _ = _single_pass_grads(enc, ls, xi, xt, pos, design, text, head)
    enc.load_state_dict(base)
    torch.manual_seed(1234)
    gi, gt, _ = _gradcache_grads_rng(enc, ls, xi, xt, pos, design, text, head, n_chunks=4)

    assert torch.allclose(gi, gi_ref, atol=1e-5), \
        f"드롭아웃 마스크가 재현되지 않았다 {(gi - gi_ref).abs().max()}"
    assert torch.allclose(gt, gt_ref, atol=1e-5)
```

`_gradcache_grads_rng`는 `_gradcache_grads`와 같되 1패스에서 청크마다 `torch.get_rng_state()`를 저장하고 2패스 직전에 `torch.set_rng_state()`로 되돌리는 버전이다. 단일 역전파 기준(`_single_pass_grads`)은 전체를 한 번에 처리하므로 청크 경계에서 마스크가 달라진다 - 이 때문에 완전 일치가 아니라 `atol=1e-5`를 쓴다. **비교가 유의미한지 반드시 확인한다**: RNG 복원 줄을 빼면 이 테스트가 실패해야 한다. 빼도 통과한다면 픽스처가 드롭아웃을 실제로 발동시키지 못하는 것이므로(`p`를 키우거나 차원을 늘려) 변별력을 확보한 뒤 진행하고, 두 결과를 리포트에 붙인다.

- [ ] **Step 3: 기존 경로가 그대로인지 확인**

Run: `./.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 158 passed, 12 skipped (기존과 동일). `grad_cache_chunks` 기본값이 1이라 다른 12개 메서드의 동작은 바뀌지 않아야 한다.

- [ ] **Step 4: 기존 방법 스모크 - 회귀가 없는지**

**반드시 Bash 툴의 `run_in_background: true`로** 실행하고 로그를 상한 20분으로 폴링한다 (포그라운드로 띄우면 턴 종료 시 프로세스가 정리되어 로그가 0바이트로 남는다 - 이 저장소에서 세 번 발생했다):

```bash
PYTHONUNBUFFERED=1 ./.venv/Scripts/python.exe scripts/run_ablation.py --arms baseline \
  --limit 400 --epochs 1 --max-steps 5 --eval-batches 1 --no-index --force
```

Expected: 예외 없이 5스텝. `grad_cache_chunks: 1`이므로 기존 경로를 타야 한다.

- [ ] **Step 5: 커밋**

```bash
git add src/train.py configs/lora_clip.yaml
git commit -m "학습 루프에 GradCache 2패스 경로 추가

grad_cache_chunks가 1이면 기존 경로 그대로, N>1이면 청크 N개를 모아
옵티마이저 스텝 1회를 만든다. pos 마스크를 연결된 전체 라벨로 만들어야
네거티브가 청크 안에 갇히지 않는다."
```

---

### Task 3: `bigbatch` 등록과 실측

**Files:**
- Create: `configs/methods/bigbatch.yaml`
- Modify: `tests/test_method_configs.py`
- Modify: `ACCURACY.md`

**Interfaces:**
- Consumes: `grad_cache_chunks` (Task 2)
- Produces: 메서드 `bigbatch`. `run_ablation.py --arms baseline bigbatch`로 실행 가능.

- [ ] **Step 1: 메서드 YAML 작성**

`configs/methods/bigbatch.yaml`:

```yaml
name: bigbatch
description: "GradCache로 유효 네거티브 32 -> 256 (baseline + 네거티브 수 축만 변경)"
extends: configs/lora_clip.yaml
data: {builder: shared, pairs: data/pairs.jsonl}
train:
  grad_cache_chunks: 8
  pk_views: 1
  locarno_aware: false
  mask_false_negatives: false
  augment: false
  img2img_weight: 0.0
```

`batch_size`는 오버라이드하지 않는다 - 32 그대로 두고 청크 8개로 유효 256을 만든다. `train` 블록의 다섯 토글은 `configs/methods/baseline.yaml`과 정확히 같아야 한다.

- [ ] **Step 2: 단일축 검증 테스트 추가**

`tests/test_method_configs.py` 끝에 추가:

```python
def test_bigbatch은_baseline과_grad_cache_chunks만_다르다():
    """네거티브 수 축 하나만 바뀌어야 Δ가 그 기여로 읽힌다."""
    c = registry.resolve("bigbatch")
    b = registry.resolve("baseline")
    exempt = ("output_dir", "grad_cache_chunks")
    ct = {k: v for k, v in c["train"].items() if k not in exempt}
    bt = {k: v for k, v in b["train"].items() if k not in exempt}
    assert ct == bt, f"train 블록에 다른 축이 섞였다: {set(ct.items()) ^ set(bt.items())}"
    assert c["lora"] == b["lora"], "용량 축은 loracap의 몫"
    assert c["model"] == b["model"], "해상도 축은 hires378의 몫"


def test_bigbatch은_batch_size를_바꾸지_않는다():
    """batch_size를 키우면 VRAM이 터지고, 줄이면 청크 수와 상쇄돼 축이 흐려진다.
    유효 배치는 오직 grad_cache_chunks로만 만든다."""
    c = registry.resolve("bigbatch")["train"]
    b = registry.resolve("baseline")["train"]
    assert c["batch_size"] == b["batch_size"]
    assert c["grad_cache_chunks"] > 1 and b["grad_cache_chunks"] == 1
```

- [ ] **Step 3: 테스트 실행**

Run: `./.venv/Scripts/python.exe -m pytest tests/ -q`
Expected: 전부 통과. 실패하면 YAML의 다섯 토글이 baseline과 다른 것이므로 YAML을 고친다(테스트를 고치지 않는다).

- [ ] **Step 4: 실측 - VRAM과 유효 배치**

`loracap`에서 batch 32가 카드에 안 들어가는 것을 며칠짜리 실행 직전에야 발견한 전례가 있으므로, 여기서는 먼저 잰다. **`run_in_background: true`로** 다음을 돌리고 폴링한다:

```bash
PYTHONUNBUFFERED=1 ./.venv/Scripts/python.exe src/train.py \
  --config outputs/methods/bigbatch/config.resolved.yaml \
  --limit 3000 --epochs 1 --max-steps 10 --eval-batches 1 --log-every 1
```

resolved config는 먼저 만든다:

```bash
./.venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'src'); import registry; print(registry.write_resolved('bigbatch'))"
```

로그에서 확인할 것:
- `eff_batch=256`이 찍히는가.
- 스텝당 시간이 baseline의 약 몇 배인가. GradCache는 순전파가 두 번이므로 이론상 약 2배가 정상이다. 그보다 훨씬 크면 스필을 의심하고 `torch.cuda.max_memory_reserved()`를 함께 재서 보고한다.
- 손실이 유한한가.

`--limit 3000`을 쓰는 이유: 유효 배치가 256이라 10스텝이면 2,560 샘플이 필요하다.

- [ ] **Step 5: `ACCURACY.md` arm 표에 추가**

`(선택)` 구역에 추가한다:

```
| (선택) `bigbatch` | baseline + GradCache로 유효 네거티브 32 -> 256 | ⑩ 대조학습의 배치 크기 민감성 |
```

실행 예시와 비용 주석도 함께 넣는다. `hobit`·`loracap` 항목과 같은 형식으로, Step 4에서 잰 스텝당 시간 배수와 VRAM을 적는다.

- [ ] **Step 6: 전체 스위트 확인 후 커밋**

```bash
git add configs/methods/bigbatch.yaml tests/test_method_configs.py ACCURACY.md
git commit -m "bigbatch 메서드 등록 - baseline 대비 유효 네거티브 수 축만 변경

batch_size는 32 그대로 두고 grad_cache_chunks 8로 유효 256을 만든다.
batch_size를 건드리면 VRAM과 축이 동시에 흔들리므로 테스트로 고정했다."
```

---

## 완료 기준

- `./.venv/Scripts/python.exe -m pytest tests/ -v` 전부 통과
- 2패스 그래디언트가 단일 역전파와 일치한다는 것이 테스트로 고정됨 (뮤테이션으로 변별력 확인)
- `grad_cache_chunks: 1`인 다른 12개 메서드의 동작이 바뀌지 않음
- 실측 로그에 `eff_batch=256`이 찍히고 VRAM·스텝 시간이 기록됨

## 알려진 한계

- **순전파가 두 번이라 스텝당 시간이 약 2배다.** 이것이 이 방법의 고정 비용이며 Δ 해석 시
  "같은 시간에 더 나은 결과"가 아니라 "같은 스텝 수에 더 많은 네거티브"로 읽어야 한다.
- **드롭아웃 때문에 RNG 상태 보존이 필수다.** 1패스와 2패스가 다른 마스크를 보면 등가성이
  깨진다. 청크별로 RNG를 저장·복원하며, 단일 역전파와 완전 일치가 아니라 청크 경계에서
  마스크 소비 순서가 달라 `atol=1e-5` 수준의 차이가 남는다.
- **`logit_scale`의 그래디언트는 1패스에서만 나온다.** 2패스는 임베딩 그래디언트만 주입하므로
  `logit_scale`이 두 번 갱신되지 않는다. 테스트가 이 값의 등가성까지 확인한다.
- **자투리 그룹을 버린다.** 에폭당 최대 `batch_size * (N-1)` 샘플이 빠진다(유효 배치 256이면
  최대 224장). 전체 42만 장 기준 0.05% 수준이다.

## 다음 단계

이 방법이 들어오면 `hires378`의 VRAM 제약이 풀린다 - 물리 배치를 8로 줄이면서 유효 네거티브
32를 유지할 수 있어 해상도 축만 바뀌는 비교가 성립한다. 스펙 §3.4의 `hires384` 표기는
`hires378`로 함께 고친다.
